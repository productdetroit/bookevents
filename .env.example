# Google Analytics 4 measurement ID, e.g. G-XXXXXXXXXX
#
# Leave unset and no analytics script loads and no consent banner renders —
# which is what you want locally and on preview deployments, so your own
# development traffic never reaches the property.
#
# Set it in Vercel for the Production environment only.
NEXT_PUBLIC_GA_MEASUREMENT_ID=
